@model List<Employee>

<div class="display-3">
    Index 3
</div>
@foreach (var emp in Model)
{
    <div class = "lead my-2">
        <h6>@emp.FullName</h6>
        <p>@emp.Id</p>
        <p>@emp.Age</p>
        <hr/>
    </div>
}